# Runtime
fastapi==0.115.5
uvicorn[standard]==0.32.1
pywebview==5.4
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.18
python-docx>=1.1,<2.0
python-dateutil>=2.9,<3.0
Pillow>=11.0,<12.0
docxtpl>=0.18,<1.0
bcrypt>=4.2,<6.0
nh3>=0.2,<1.0  # HTML sanitiser for inbound email bodies (stored-XSS guard)

# OCR / document extraction
PyMuPDF>=1.24,<2.0
pytesseract>=0.3.13,<0.4
rapidfuzz>=3.9,<4.0
mrz>=0.6.2,<1.0  # passport MRZ (TD3) parser for OCR passport extraction

# DOCX -> PDF conversion (Word COM). The PdfChain tries docx2pdf, then
# win32com (pywin32), then a PowerShell-COM shell-out. Without these two
# packages installed ONLY the slow PowerShell fallback works — every preview
# then shells out to Word (~6s/doc) instead of converting in-process. Both are
# Windows-only (the host requires Microsoft Word for any of the methods).
docx2pdf>=0.1.8; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"

# Dev / quality
ruff>=0.7,<1.0
mypy>=1.13,<2.0
pytest>=8.3,<9.0
pytest-cov>=6.0,<7.0
httpx>=0.27,<1.0
cryptography==48.0.0
pywebpush==2.3.0
