"""Pure-Python business logic ported from gssg_manager.pyw v3.5.4.

No FastAPI, SQLAlchemy, Tkinter, or PySide imports in this package — only the
domain rules. Higher layers (`app.services`, `app.api`) wire these into the
HTTP surface.
"""
